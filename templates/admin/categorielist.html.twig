{% extends 'base.html.twig' %}

{% block title %} ADMINISTRATION - LISTES DE CATÉGORIES {% endblock %}

{% block body %}


<div class="container-fluid">
    <div class="admin">
        <div class="row">
            <div class="col-12 col-md-8 offset-md-2">
            {% for categorie in categories %}

            <li class="color"><a href="{{path('admin_categorie_edit', {'id': categorie.id }) }}">
                    {{ categorie.nom}} </a></li>
                </div>
            {% endfor %}
        </div>
    
</div>
</div>

{% endblock %}